<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mortal.wms.business.mapper.OrderMapper">
<select id="list">
    select * from `orders` o
             left join customer_info ci on o.customer_info_id = ci.id
             where o.deleted_time is null
    <if test="companyName!=null and companyName!=''">
        and ci.company_name like concat('%',#{companyName},'%')
    </if>
    order by o.order_creation_time desc
</select>
    <select id="getOweOrder"
    >
        select * from `orders` o
        left join customer_info ci on o.customer_info_id = ci.id
        where o.deleted_time is null and owe = 0
        <if test="companyName !=null and company!='' ">
            and ci.company_name like concat('%',#{companyName},'%')
        </if>
        <if test="startDate !=null and startDate != '' ">
            and o.order_creation_time >= #{startDate}
        </if>
        <if test="endDate !=null and endDate != ''">
            and o.order_creation_time &lt; #{endDate}
        </if>
    </select>
</mapper>